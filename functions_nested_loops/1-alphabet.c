#include <main.h>
/**
 * main - print alphabet
 * Return: Always 0
 */
void print_alphabet(void)
{
	char letter;

	letter = ('a')

	while (letter <= 'z')
	{
		ft_putchar(letter);
		letter++;
	}
	_putchar('\n');
	return (0);
}
